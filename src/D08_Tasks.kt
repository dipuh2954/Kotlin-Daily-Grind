fun main(){
//    E1 Declare a String? variable. Use  ?. to safely print its length. Use ?: to default to 0 if null. Try both with null and a real string.

//    E2 Write a function greetOrDefault(name: String?): String that returns "Hello, {name}!" if name is not null, or "Hello, Stranger!" if null. Use ?:.
//    E3 Use ?.let to print a user's email in uppercase — but only if the email is not null. Do nothing (silently skip) if null.
//    E4 Write fun safeParseInt(input: String?): Int that uses toIntOrNull() and Elvis to return 0 for any unparseable input.
//    E5 Model a nullable address chain: User? → address: Address? → city: String?. Access the city safely in one line using chained ?. and print "Unknown City" if any part is null.
//    E6 Demonstrate smart cast: declare a String?, check if it's not null with an if, and inside the block use .length and .uppercase() directly without ?..

}